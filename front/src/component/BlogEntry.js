import React from 'react';

const BlogEntry = ({ blog, type }) => {
  if (blog === false) {
    return (
      <span>--- DELETED ---</span>
    );
  }

  return (
    <div>
      
    </div>
  );
};

export default BlogEntry;
